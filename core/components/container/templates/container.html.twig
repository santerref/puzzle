{% if component.children.where('position', component.position).count %}
    {% for child in component.children.where('position',component.position).all %}
        {% if child.children %}
            {{ render(child) }}
        {% else %}
            {{ child.rendered_html|raw }}
        {% endif %}
    {% endfor %}
{% endif %}
