{% if context.component.children.where('position', context.component.position).count %}
    {% for child in context.component.children.where('position', context.component.position).all %}
        {% if child.children %}
            {{ render(child) }}
        {% else %}
            {{ child.rendered_html|raw }}
        {% endif %}
    {% endfor %}
{% endif %}
