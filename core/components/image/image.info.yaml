name: Image
version: 0.1.0
template: templates/image.html.twig
settings:
    css:
        variables:
            left: 'tw:justify-start'
            center: 'tw:justify-center'
            right: 'tw:justify-end'
fields:
    image:
        label: Image
        type: media
        settings:
            image_only: true
            cardinality: 1
    position:
        label: Image position
        type: select
        default_value: left
        settings:
            options:
                left: Left
                center: Center
                right: Right
