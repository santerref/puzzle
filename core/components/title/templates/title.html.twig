{% set classes = [
    'title',
    'title--' ~ css.variables[component.fields.level]
]|css %}
<{{ component.fields.level }} class="{{ classes }}">{{ component.fields.text }}</{{ component.fields.level }}>
